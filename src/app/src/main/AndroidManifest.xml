<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BoardGameApp">
        <activity
            android:name=".NewDateActivity"
            android:exported="false" /> <!-- Neuer Termin -->
        <activity
            android:name=".VotingActivity"
            android:exported="false" /> <!-- Registrierungsseite -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" /> <!-- Hauptseite der App -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- Seite für neue Spiele -->
        <activity
            android:name=".NewGameActivity"
            android:exported="false" /> <!-- Chat-Seite -->
        <activity
            android:name=".ChatActivity"
            android:exported="false" /> <!-- Einstellungsseite -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" /> <!-- Startseite der App -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">   <!-- Login Seite -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
